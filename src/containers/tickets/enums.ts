export enum ETransfers {
  all = 'все',
  direct = '0',
  oneStop = '1',
  twoStop = '2',
  threeStop = '3',
}

export enum EOnly {
  only = 'ТОЛЬКО',
}

export enum ECarrier {
  tk = 'TK',
  ba = 'BA',
  su = 'SU',
  s7 = 'S7',
}
